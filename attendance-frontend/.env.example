# Copy to .env for local development.
# The backend defaults to port 5001 (macOS reserves 5000 for AirPlay Receiver).
VITE_API_URL=http://localhost:5001/api

# Optional: derived from VITE_API_URL when omitted.
VITE_WS_URL=ws://localhost:5001
